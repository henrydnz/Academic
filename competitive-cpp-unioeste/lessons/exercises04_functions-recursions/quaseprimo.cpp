#include <bits/stdc++.h>
using namespace std;

#define ll long long

ll countPrime(int n) {

    ll counter = 0, k = n;

    for (i = n; i > 1; i--){
        
    }
    return counter;

}


int main() {
//     ll n; int k;                            // 1e9 eh longlong, declara n, qtd de primo
//     cin >> n >> k;
//     vector<int> p (k);                      // declara vetor de primos de tamanho k 

//     for (int& x : p) cin >> x;              // input primos
    int a; cin >> a;
    int x = countPrime(a);
    cout << x;


    return 0;
}