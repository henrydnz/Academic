#include <stdio.h>

int main() {
    int x; 
    scanf("%d", &x);

    // UMA VEZ
    print("Feliz nat");
    
    // loop p printar a pode ser while e for
    while(x--) printf("a");

    // UMA  VEZ (final)    
    printf("l!\n");
    
    return 0;
}